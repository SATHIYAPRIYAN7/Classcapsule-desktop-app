// API Configuration for Renderer Process
const config = {
 API_BASE_URL: 'https://api-dev-classcapsule.nfndev.com',
 // API_BASE_URL: 'https://class-capsule-2.onrender.com',
  NODE_ENV: 'development'
};

// Make config available globally for renderer process
window.APP_CONFIG = config; 